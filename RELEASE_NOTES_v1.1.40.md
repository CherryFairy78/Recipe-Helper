# Recipe Helper v1.1.40

- Fixed subfolder renaming so changing a folder name keeps it inside its existing parent folder.
- The Rename dialog now displays the parent folder and prevents duplicate folder names at the same level.
