# Recipe Helper v1.1.22

## What's New

- Fixed a gatherable job lookup crash that could happen while filtering gatherables or collectables.
- Added a safer fallback when gathering point subcategory data is unavailable, so the search filters keep working instead of breaking the window.

## Notes For Publish

- Release asset name: `DalamudRecipeHelper-v1.1.22.zip`
- Repo manifest target: `v1.1.22`
